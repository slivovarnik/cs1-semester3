# Service account for HR backend pods
# No IRSA annotation needed for k3s
# The EC2 instance profile on the k3s node provides
# Secrets Manager access to all pods on the instance
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hr-backend-sa
  namespace: hr-app